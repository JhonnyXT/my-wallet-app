---
description: Sincroniza la documentación del proyecto con el código real y luego sube los commits pendientes a origin (última verificación antes de que el código quede definitivo)
---

Lee y sigue `.agents/workflows/push.md` — la fuente única de este comando, compartida con Cursor.
