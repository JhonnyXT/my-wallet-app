---
description: Compila el APK release de MyWallet y lo instala en el dispositivo vía ADB
---

Lee y sigue `.agents/workflows/build-apk.md` — la fuente única de este comando, compartida con Claude Code.

> `/arrancar` y `/build-apk` son el mismo procedimiento (compilar release + instalar por ADB); se conservan ambos nombres por costumbre, con una sola fuente.
